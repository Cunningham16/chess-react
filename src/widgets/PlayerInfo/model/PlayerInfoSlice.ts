import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    fallenFiguresWhite: [],
    fallenFiguresBlack: [],
    
}

const slice = createSlice({
    name: "PlayerInfo",
    initialState: 0,
    reducers: {

    }
})